# Day 4 — gateway serving deps (the GATEWAY, not the engine).
# The engine (vLLM / llama.cpp) runs as a separate process/container.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pydantic>=2.9.0
pyyaml>=6.0
redis>=5.0.0
prometheus-client>=0.20.0
pytest-asyncio>=0.23
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
faiss-cpu>=1.8.0
