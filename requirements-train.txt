# Day 2 — GPU training dependencies.
# Install these on the GPU box (Colab / Kaggle / RunPod), NOT in the Day 1 env.
# Pinned loosely; a training pod is disposable so exact reproducibility comes
# from config + data version, and these floors avoid known-broken combos.
torch>=2.3.0
transformers>=4.46.0
datasets>=3.0.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0        # 4-bit quantization; CUDA only (no macOS/CPU)
accelerate>=1.0.0
mlflow>=2.16.0
sentencepiece>=0.2.0
scipy>=1.13.0
