apiVersion: v1
kind: Service
metadata:
  name: domainbot-redis
  namespace: domainbot
spec:
  selector: { app: domainbot, component: redis }
  ports: [{ port: 6379, targetPort: 6379 }]
  type: ClusterIP
