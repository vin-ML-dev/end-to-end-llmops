apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards-provider
  namespace: domainbot
data:
  provider.yml: |
    apiVersion: 1
    providers:
      - name: domainbot
        folder: DomainBot
        type: file
        options:
          path: /var/lib/grafana/dashboards
